package com.github.samarium150.structurescompass.utils;

import net.minecraftforge.common.WorldWorkerManager;

public class StructuresSearchWorker implements WorldWorkerManager.IWorker {
    
    @Override
    public boolean hasWork() {
        return false;
    }
    
    @Override
    public boolean doWork() {
        if (hasWork()) {
        
        }
        return false;
    }
}
